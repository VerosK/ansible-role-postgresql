# {{ ansible_managed }}

# This file is autogenerated by Ansible. Don't touch.

{# Create config from __default config, postgresql_config and postgresql_extra_config #}
{% set config = __postgresql_default_config -%}
{%- for key, item in config|combine(postgresql_config,postgresql_extra_config)| dictsort -%}
{%- if item is mapping -%}
# {{ item.comment }}
{{ key }} = '{{ item.value }}'
{% else -%}
{{ key }} = '{{ item }}'
{% endif -%}
{% endfor -%}